<nav class="navbar navbar-expand-lg navbar-dark bg-transparent">
  <div class="container">
    <%= link_to 'First App', root_path, class: 'navbar-brand text-dark' %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
       <% if user_signed_in? %>
        <li class="nav-item active ">
            <%= link_to 'My Articles', articles_path, class: 'nav-link text-dark' %>
        </li>
       <% end %>
       <li class="nav-item">
         <%= link_to 'New Articles', new_article_path, class: 'nav-link text-dark' %>
       </li>
       <% if user_signed_in? %>
        <% if current_user.role=='admin' %>
        <li class="nav-item">
          <%= link_to 'All Users', users_path, class: 'nav-link' %>
        </li>  
        <% end %>
      <% end %>

      </ul>
      <div class="d-flex align-items-center ms-auto ">
        <ul clas="">
          <% if user_signed_in? %>
            Logged in  <strong><%= current_user.name %></strong>
            <%= link_to 'My profile', current_user, :class => 'btn btn-primary me-3' %>
            <%= link_to "Logout", destroy_user_session_path, :class => 'btn btn-primary me-3'%></li>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-primary me-3'  %>
            <%= link_to "Login", new_user_session_path, :class => 'btn btn-primary me-3'  %>
          <% end %>
        </ul>
      </div>


    </div>
  </div>
</nav>